# 이 파일을 `.env` 로 복사하고 실제 키를 넣는다. `.env` 는 .gitignore 대상이다.
#   cp .env.example .env && chmod 600 .env
#
# 없으면 agent_node 가 기동조차 못 한다 (src/vla_system/vla_system/agent/llm.py).
OPENAI_API_KEY=replace_with_your_openai_api_key
